# 🔐 MutanoX API Subscription System

Sistema completo de gerenciamento de API Keys com assinaturas, dashboard administrativo e segurança em múltiplas camadas.

## 🚀 Instalação

1. Clone o repo
2. `npm install`
3. Configure `.env`
4. `npm start`

Acesse o dashboard em: `/api/dashboard/apikeys`
